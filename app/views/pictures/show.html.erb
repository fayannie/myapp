<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @picture.title %>
  <b>Photo:</b>
  <%= image_tag @picture.photo.url %>
  <%= image_tag @picture.photo.url(:small) %>
</p>


<%= link_to 'Edit', edit_picture_path(@picture) %> |
<%= link_to 'Back', pictures_path %>
